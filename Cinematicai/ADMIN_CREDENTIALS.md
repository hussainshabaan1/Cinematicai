# 🔐 بيانات دخول Admin - CinematicAI

## بيانات الدخول الافتراضية

عند رفع المشروع لأول مرة، استخدم هذه البيانات لإنشاء حساب Admin:

```
البريد الإلكتروني: admin@cinematicai.com
كلمة المرور: Admin@2025
اسم المستخدم: admin
```

---

## ✅ خطوات إنشاء حساب Admin

### 1. افتح التطبيق
اذهب إلى رابط التطبيق بعد النشر:
```
https://your-project.onspace.app
# أو
https://your-custom-domain.com
```

### 2. التسجيل كأول مستخدم
1. اضغط على **"تسجيل جديد"** أو **"Sign Up"**
2. أدخل البيانات التالية:
   - **البريد الإلكتروني:** `admin@cinematicai.com`
   - **كلمة المرور:** `Admin@2025`
   - **اسم المستخدم:** `admin`
3. اضغط **"إرسال رمز التحقق"**

### 3. التحقق عبر OTP
1. افتح البريد الإلكتروني `admin@cinematicai.com`
2. ستجد رسالة من المنصة تحتوي على **رمز مكون من 4 أرقام**
3. أدخل الرمز في صفحة التسجيل
4. اضغط **"تأكيد"**

### 4. إكمال التسجيل
1. أدخل كلمة المرور: `Admin@2025`
2. اضغط **"إنشاء الحساب"**
3. 🎉 **تم! أنت الآن Admin**

---

## 🔑 الوصول إلى لوحة تحكم Admin

بعد تسجيل الدخول:

### الطريقة 1: الرابط المباشر
```
https://your-domain.com/admin/api-keys
```

### الطريقة 2: عبر الواجهة
1. سجل دخول إلى الحساب
2. في شريط العنوان، اكتب: `/admin/api-keys`
3. ستفتح صفحة إدارة مفاتيح API

---

## 🔧 إدارة مفاتيح API

في صفحة Admin، يمكنك:

### ✅ إضافة مفاتيح جديدة

1. اضغط **"إضافة مفتاح جديد"**
2. اختر الخدمة:
   - **Sora2API** (توليد الفيديو)
   - **AtlasCloud** (تحليل النص)
3. أدخل قيمة المفتاح
4. اضغط **"إضافة المفتاح"**

### 📊 مراقبة المفاتيح

كل مفتاح يعرض:
- **الحالة:** نشط / معطل
- **عدد الفشل:** كم مرة فشل المفتاح
- **آخر استخدام:** تاريخ آخر استخدام للمفتاح

### 🔄 تفعيل/تعطيل المفتاح

- اضغط على أيقونة **⚡** لتعطيل المفتاح
- اضغط على أيقونة **⚪** لتفعيل المفتاح

### 🗑️ حذف المفتاح

- اضغط على أيقونة **🗑️**
- أكد الحذف

---

## 🛡️ الأمان

### ⚠️ تحذيرات هامة

1. **غيّر كلمة المرور فوراً!**
   - بعد أول تسجيل دخول، اذهب إلى الإعدادات وغيّر الباسورد
   - استخدم كلمة مرور قوية (12 حرف على الأقل، أرقام ورموز)

2. **لا تشارك بيانات Admin**
   - بيانات Admin حساسة جداً
   - فقط المسؤولون الموثوقون يجب أن يكون لديهم صلاحية Admin

3. **استخدم بريد إلكتروني حقيقي**
   - إذا فقدت الوصول، يمكنك استرجاعه عبر البريد

### 🔐 تأمين إضافي

#### تعيين Admin إضافي يدوياً:

إذا أردت تعيين مستخدم آخر كـ Admin:

```sql
-- على OnSpace Cloud: Cloud -> Data -> SQL Editor
-- على Supabase: Database -> SQL Editor

UPDATE public.user_profiles 
SET role = 'admin' 
WHERE email = 'another-admin@example.com';
```

#### التحقق من قائمة Admins:

```sql
SELECT id, email, username, role, created_at
FROM public.user_profiles 
WHERE role = 'admin'
ORDER BY created_at ASC;
```

#### إزالة صلاحية Admin:

```sql
UPDATE public.user_profiles 
SET role = 'user' 
WHERE email = 'user-to-demote@example.com';
```

---

## 🤖 كيف يعمل النظام تلقائياً؟

### Trigger تلقائي في قاعدة البيانات

عند تنفيذ `schema.sql`، يتم إنشاء Trigger يعمل كالتالي:

```sql
CREATE FUNCTION auto_assign_first_admin()
RETURNS TRIGGER AS $$
BEGIN
  -- إذا لم يوجد أي Admin
  IF NOT EXISTS (SELECT 1 FROM public.user_profiles WHERE role = 'admin') THEN
    -- اجعل هذا المستخدم Admin
    NEW.role := 'admin';
  END IF;
  
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trigger_auto_assign_first_admin
  BEFORE INSERT ON public.user_profiles
  FOR EACH ROW
  EXECUTE FUNCTION auto_assign_first_admin();
```

**النتيجة:**
- ✅ **أول مستخدم يسجل → يصبح Admin تلقائياً**
- ✅ **باقي المستخدمين → يصبحون users عاديين**

---

## ❓ الأسئلة الشائعة

### س: ماذا لو سجل شخص آخر قبلي؟

**ج:** سيصبح هو Admin! لتجنب ذلك:
1. قم بإعداد قاعدة البيانات
2. فوراً سجل حساب Admin
3. أضف مفاتيح API قبل مشاركة الرابط

### س: هل يمكن أن يكون أكثر من Admin؟

**ج:** نعم! استخدم SQL لتعيين admins إضافيين:
```sql
UPDATE user_profiles SET role = 'admin' WHERE email = 'admin2@example.com';
```

### س: نسيت كلمة مرور Admin، ماذا أفعل؟

**ج:** استخدم خاصية "نسيت كلمة المرور" في صفحة تسجيل الدخول، أو أنشئ admin جديد عبر SQL.

### س: كيف أعرف أنني Admin؟

**ج:** جرب الدخول إلى `/admin/api-keys`. إذا نجحت، أنت Admin!

---

## 📞 الدعم

إذا واجهت مشاكل:

1. **راجع Logs:**
   - OnSpace: Cloud -> Log
   - Supabase: Dashboard -> Logs

2. **تحقق من قاعدة البيانات:**
   ```sql
   SELECT * FROM user_profiles WHERE role = 'admin';
   ```

3. **اتصل بالدعم:**
   - Email: contact@onspace.ai

---

## ✅ قائمة التحقق

قبل مشاركة المشروع:

- [ ] تم إنشاء حساب Admin بنجاح
- [ ] تم تغيير كلمة المرور الافتراضية
- [ ] تم الوصول إلى `/admin/api-keys` بنجاح
- [ ] تم إضافة مفاتيح API (Sora2API + AtlasCloud)
- [ ] تم اختبار إضافة/حذف/تعطيل المفاتيح
- [ ] تم اختبار توليد فيديو بنجاح

---

<div align="center">

**🔐 احفظ هذه البيانات في مكان آمن!**

[الوثائق](./README.md) • [دليل النشر](./DEPLOYMENT.md) • [الدعم](mailto:contact@onspace.ai)

</div>
